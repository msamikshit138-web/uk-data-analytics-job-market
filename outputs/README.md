# Generated Outputs

This folder is populated by the pipeline.

Run:

```bash
python src/run_pipeline.py --public
```

For the optional live-vacancy layer:

```bash
python src/run_pipeline.py --adzuna
```

No synthetic market findings are committed here.
